
    #include <stdio.h>
    #include <stdlib.h>
    
    int main()
    {
        int n;
        scanf("%d",&n);
        
        
        int a,b=0,c=0i;
        for(i=1,i<=n,i++);
        {
            scanf("%d",%a);
            if(a%2==0)
                b++;
              else 
                  c++;
        }
        
        if (b>=c)
        printf(" Ready for war");
        else
        printf("Not ready for war");
        
        
        return 0;
    }

