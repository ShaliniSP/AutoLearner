
    #include<stdio.h>
    void main()
    {
    int n,a,b,p[200];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    if(p[i]%2==0)
    a++
    if(p[i]%2!=0)
    b++;
    }
    if (a>b)
    printf("READY FOR BATTLE");
    else("NOT READY");
    } 

