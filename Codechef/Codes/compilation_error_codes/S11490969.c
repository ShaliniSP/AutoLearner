
    #include<stdio.h>
    void main()
    {
    int n,a,b,a[200];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    if(a[i]%2==0)
    a++
    if(a[i]%2!=0)
    b++;
    }
    if (a>b)
    printf("READY FOR BATTLE");
    else("NOT READY");
    }

