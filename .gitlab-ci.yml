# This file is a template, and might need editing before it works on your project.
# Full project: https://gitlab.com/pages/plain-html
#pages:
#  stage: deploy
#  script:
#    - mkdir .public
#    - cp -r * .public
#    - mv .public public
#  artifacts:
#    paths:
#      - public
#  only:
#    - master

before_script:
  - apt-get install docker
  - docker info

build_image:
  script:
    - docker build --rm=true -t clara .
    - docker run -ti -h clara --name clara demo/demo_newclara